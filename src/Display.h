#ifndef ARBREBINAIRE
#include "ArbreBinaire.h"
#endif

/**
 * Saves a binary tree in a file whose name is given in parameter.
 * The file is saved under the gv format.
 */
void save_tree_in_file (binary_tree_p ,char *) ;
